<?xml version="1.0" encoding="utf-8"?>
<!-- allowBackup is disabled and no cloud backup agent is used: this app never relies on
     Android's automatic cloud backup for financial data. Backups are only created explicitly
     via the in-app encrypted export (see backup/BackupManager.kt). -->
<data-extraction-rules>
    <cloud-backup disableIfNoEncryptionCapabilities="true">
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
